#Download prezto
git clone --recursive https://github.com/sorin-ionescu/prezto.git "${ZDOTDIR:-$HOME}/.zprezto"

#Install rust
curl -f https://sh.rustup.rs | sh

#Create symlink to steel alphabet font
sudo cp ~/.config/myresources/fonts/Steel_Alphabet.ttf /usr/share/fonts/TTF/
sudo chmod 0644 /usr/share/fonts/TTF/Steel_Alphabet.ttf
pushd /usr/share/fonts/TTF/
sudo mkfontscale
sudo mkfontdir
popd
sudo fc-cache -vf 


echo '
Things ya gotta do:
Install monaco font using package provider
Install i3 gaps
Install compton
Install urxvt
Install autocutsel
Install neovim  + Neovim plug (And then plugupdate and stuff)
Install rofi
Probably some other stuff lol
' > ~/STYLINTODO.txt && cat STYLINTODO
